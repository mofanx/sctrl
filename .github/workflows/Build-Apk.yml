name: Build-Apk

on:
  workflow_dispatch:

  push:
    branches:
      - '**'
    paths-ignore:
      - 'LICENSE'
      - '*.md'
      - '.github/**'

jobs:
  build:
    if: ${{ !startsWith(github.event.head_commit.message, 'chore:') && !startsWith(github.event.head_commit.message, 'chore(') }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-java@v5
        with:
          distribution: 'zulu'
          java-version: '21'

      - uses: gradle/actions/setup-gradle@v5
        with:
          cache-encryption-key: ${{ secrets.GRADLE_CACHE_ENCRYPTION_KEY }}

      - name: write ank secrets info
        run: |
          echo ${{ secrets.ANK_STORE_FILE_BASE64 }} | base64 --decode > ${{ github.workspace }}/ank.jks
          echo ANK_STORE_FILE='${{ github.workspace }}/ank.jks' >> gradle.properties
          echo ANK_STORE_PASSWORD='${{ secrets.ANK_STORE_PASSWORD }}' >> gradle.properties
          echo ANK_KEY_ALIAS='${{ secrets.ANK_KEY_ALIAS }}' >> gradle.properties
          echo ANK_KEY_PASSWORD='${{ secrets.ANK_KEY_PASSWORD }}' >> gradle.properties

      - run: echo ANK_RENAME_APK_FLAG=1 >> gradle.properties

      - run: chmod 777 ./gradlew
      - run: ./gradlew app:assembleAnkRelease

      - uses: actions/upload-artifact@v7
        with:
          archive: false
          path: app/build/outputs/apk/ank/release/*.apk

      - uses: actions/upload-artifact@v7
        with:
          name: outputs
          path: app/build/outputs
